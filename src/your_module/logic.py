import pandas as pd

def sample_function(data: pd.DataFrame) -> pd.DataFrame:
    """
    Example function that returns the same dataframe.

    Parameters:
        data (pd.DataFrame): Input dataframe

    Returns:
        pd.DataFrame: Same as input
    """
    return data
